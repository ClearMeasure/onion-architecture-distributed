@model Core.Applicant[]
           
<div style="text-align:left">
<h3>Recent Applicants</h3>
    @foreach (var visitor in ViewData.Model){
        @visitor.VisitDate @:-
        @visitor.IpAddress @:-
        @visitor.LoginName @:-
        @visitor.PathAndQuerystring <br />
        @visitor.Browser <hr />
    }
</div>
